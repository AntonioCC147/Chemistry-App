'use strict';

var React = require('react')
var Button = require('./src')
var ReactButton = require('react-button')

function onMenuClick(event, itemProps){
    console.log('Clicked', itemProps.data.label)
}

var saveItems = [
    {
        label: 'Save as',
        items: [
            {label: 'ODF'},
            {label: 'Docx'}
        ]
    },
    '-',
    {
        label: 'Export',
        onClick: function(){
            console.log('export!')
        }
    }
]

var openItems = [
    {
        label: 'From remote server'
    },
    {
        label: 'From local drive'
    },
    {
        label: 'From email'
    }
]

var App = React.createClass({

    onChange: function(value){
        VALUE = value
        this.setState({})
    },

    render: function() {

        var style = {
            width: '50%'
        }

        function openClick(){
            console.log('clicked open')
        }

        return (
            <div className="App" style={{padding: 10}}>
                <ReactButton>Simple button</ReactButton>
                <Button commonStyle={{color: 'red'}} items={saveItems} arrowPosition='left' onMenuClick={onMenuClick} >
                    Save
                </Button>
                <Button href="#open" onClick={openClick} onMenuClick={onMenuClick} items={openItems}>
                    Open
                </Button>
            </div>
        )
    }
})

React.render(<App />, document.getElementById('content'))